# Netlify deploy config for Market Map.
# Static site — no build step. Publishes the repo root as-is.

[build]
  publish = "."
  command = ""

# index.html (Summary) is the site root; sub-pages resolve directly
# (/insights.html, /earnings.html, /trades.html, /frag/*).
