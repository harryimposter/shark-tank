# Market Map has NO third-party dependencies — Python standard library only.
# Requires Python 3.9+. There is nothing to install.
#
# The brief is generated by Claude Code (web searches + PROMPT.md), not by an
# API call from this project, so the `anthropic` package is no longer needed.
